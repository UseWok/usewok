{
  "name": "Conversation",
  "type": "object",
  "properties": {
    "conv_id": {
      "type": "string",
      "title": "Conversation ID"
    },
    "title": {
      "type": "string",
      "title": "Title"
    },
    "preview": {
      "type": "string",
      "title": "Preview"
    },
    "model": {
      "type": "string",
      "title": "Model"
    },
    "agent": {
      "type": "string",
      "title": "Agent"
    },
    "messages_json": {
      "type": "string",
      "title": "Messages JSON",
      "format": "textarea"
    },
    "description": {
      "type": "string",
      "maxLength": 1000
    }
  },
  "required": [
    "conv_id"
  ],
  "rls": {
    "create": true,
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "write": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}