/**
 * ChatOverlay — removed. Chat is now full-screen (100vw × 100vh).
 * This file is kept as a passthrough for import compatibility.
 */
export default function ChatOverlay({ children }) {
  return children;
}